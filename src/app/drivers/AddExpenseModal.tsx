interface ExpenseData {
  // Define appropriate types for your expense data
  amount: number;
  description: string;
  date: Date;
  // Add other relevant fields
}

// Replace any with proper type
const handleSubmit = (data: ExpenseData) => {
  // ... existing code ...
}

// Remove unused driverId or use it
// If you need to keep it for future use, you can comment it out
// const driverId = ...
// ... existing code ... 